# Cypress Testing for Simple Requests

- Tests assume Foundry VTT is running and accessible
- Optimized for local development, not CI/CD
- Quench in-game tests: `npm run test:quench` (see [`quench/README.md`](../quench/README.md))